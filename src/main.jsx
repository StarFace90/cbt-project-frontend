import React from "react";
import ReactDOM from "react-dom/client";
import CBTApp from "./App"; // App.jsx 파일

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CBTApp />
  </React.StrictMode>
);
